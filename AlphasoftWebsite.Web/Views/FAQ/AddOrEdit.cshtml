@model AlphasoftWebsite.Core.Model.FAQ
@{
    ViewBag.Title = "AddOrEdit";
    Layout = null;
}
@using (Html.BeginForm("AddOrEdit", "FAQ", FormMethod.Post, new { enctype = "multipart/form-data", onSubmit = "return jQueryAjaxPost(this);", data_restUrl = Url.Action("AddOrEdit", "FAQ", new { id = 0 }) }))//data-reset
{
    @Html.AntiForgeryToken()
    <section class="content">
        <div class="row">
            <div class="col-md-12">
                <div class="box box-info">
                    <div class="box-header">
                        <h3 class="box-title">
                            Frequently Ask Questions
                        </h3>
                        <hr />
                        <div class="form-horizontal ">
                            @Html.ValidationSummary(true)
                            @Html.HiddenFor(model => model.FAQId)
                            @Html.HiddenFor(model => model.CreatedBy)
                            @Html.HiddenFor(model => model.CreatedDate)


                            <div class="form-group">
                                @Html.LabelFor(model => model.FAQQuestion, "Frequently Asked Questions", new { @class = "control-label col-md-4" })
                                <div class="col-md-8">
                                    @Html.EditorFor(model => model.FAQQuestion, new { htmlAttributes = new { @class = "form-control", @id = "FAQQuestion" } })
                                    @Html.ValidationMessageFor(model => model.FAQQuestion)
                                </div>
                            </div>

                            <div class="form-group">
                                @Html.LabelFor(model => model.FAQAnswer, "Frequently Asked Question Answer", new { @class = "control-label col-md-4" })
                                <div class="col-md-8">
                                    @Html.EditorFor(model => model.FAQAnswer, new { htmlAttributes = new { @class = "form-control", @id = "FAQAnswer" } })
                                    @Html.ValidationMessageFor(model => model.FAQAnswer)
                                </div>
                            </div>

                            <div class="form-group">
                                @Html.LabelFor(model => model.IsActive,"Is Active", new { @class = "control-label col-md-4" })
                                <div class="col-md-8">
                                    @Html.CheckBoxFor(model => model.IsActive, new { htmlAttributes = new { @class = "form-control" } })
                                    @Html.ValidationMessageFor(model => model.IsActive)
                                </div>
                            </div>

                            <div class="form-group">
                                <div class="col-md-offset-4 col-md-8">
                                    <input type="submit" value="Submit" class="btn btn-success" />
                                    @if (Model.FAQId != 0)
                                    {
                                        <a class="btn btn-default" onclick="refreshAddNewTab('@Url.Action("AddOrEdit","FAQ",new{id=0})', false)"><i class="fa fa-plus"></i> Add New</a>
                                    }
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
}